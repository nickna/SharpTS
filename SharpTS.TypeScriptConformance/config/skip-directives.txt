# Directives whose presence wholesale skips the test (bucketed as Skipped:directive:<name>).
# Lower-case the directive name (matches RawDirectives key normalization).
# Lines starting with # and blank lines are ignored.

# Decorator support varies; bucketing as Skipped is more honest than running and failing.
experimentaldecorators

# Class fields semantics edge case; not in scope for Phase 1.
usedefineforclassfields

# Per-file isolation mode; not relevant to type-only conformance.
isolatedmodules

# allowJs / checkJs — JS-in-TS testing surface, not what we're testing here.
allowjs
checkjs
